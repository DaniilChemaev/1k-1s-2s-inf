ЛИНЕЙНЫЙ ОДНОСВЯЗНЫЙ СПИСОК

Хранится само значение и значение где лежит следующий элемент

Включает в себя тип данных, значение и ссылку

class Elem {
    int info;
    Elem next;  // link to next elem
}

[][] -> [][] -> [][] -> [][] -> null

head (первый элемент)
Потеряем первый элемент -- потеряем весь список

Самый простой способ создать список -- задом-наперед

Сложность доступа -- линейная
Нужно понять что мы будет делать чаще: объединять или искать по индексу

При обработке списка, если мы собираемся что-то изменять или удалять, мы смотрим элемент, но проверяем следующий

УДАЛЕНИЕ:

// q = p.next;
p.next = p.next.next;
// delete q;

ВСТАВКА:

1. В начало = создание списка

2. Последним
    p = head
    while (p.next != null){
        p = p.next;
    }
3. head 
    q.next = p.next
    p.next = q

4. Вставить 